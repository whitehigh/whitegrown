# Upload status

Created by Codex on 2026-05-07.

Uploaded through the GitHub connector:

- `README.md`
- `package.json`
- `.gitignore`

Local repository prepared at `D:\爬数据`:

- branch: `main`
- latest commit: `b8bd48f Use English README for GitHub upload`

The full local project is committed and ready to push, but GitHub CLI authentication is still blocked because `gh auth status` reports an invalid `whitehigh` keyring token and the token is not being saved after web authorization.

Paths intentionally excluded from upload:

- `.douyin-profile/`
- `node_modules/`
- `out/`
